## Check Python Syntax Without Running the File

Use this command to verify that a `.py` file has valid Python syntax without executing the script:

```bash
python3 -m py_compile your_file.py
```

If `python3 -m py_compile your_file.py` produces **no output** and just returns to the prompt, that means the file is **syntactically valid Python**.

